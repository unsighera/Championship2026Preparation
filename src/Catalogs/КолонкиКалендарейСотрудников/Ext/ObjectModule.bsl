

&После("ПередЗаписью")
Процедура пр_ПередЗаписью(Отказ)
	
	Если пр_VIPЛимит > 0 Тогда
	
		Наименование = пр_РаботаСКонтактЦентром.ПредставлениеКолонкиАнализа(Ссылка, пр_VIPЛимит);
	
	КонецЕсли;
	
КонецПроцедуры

